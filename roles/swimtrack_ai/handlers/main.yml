---
- name: Restart SwimTrack AI
  ansible.builtin.systemd_service:
    name: "{{ swimtrack_ai_service_name }}"
    scope: user
    daemon_reload: true
    enabled: true
    state: restarted
  environment: "{{ swimtrack_ai_systemd_environment }}"
