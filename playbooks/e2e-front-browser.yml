---
- name: Exercise the published SwimTrack Front in a real browser
  hosts: front_hosts
  gather_facts: false
  any_errors_fatal: true
  serial: 1
  tasks:
    - name: Run the published browser E2E suite
      ansible.builtin.include_role:
        name: swimtrack_front
        tasks_from: e2e_browser
