---
- name: Publish the SwimTrack front through Apache
  hosts: front_hosts
  gather_facts: false
  any_errors_fatal: true
  become: true
  tasks:
    - name: Configure the existing SwimTrack Apache proxy
      ansible.builtin.include_role:
        name: swimtrack_front
        tasks_from: apache
