---
- name: Smoke test the deployed SwimTrack front
  hosts: front_hosts
  gather_facts: true
  any_errors_fatal: true
  tasks:
    - name: Run the SwimTrack front smoke test
      ansible.builtin.include_role:
        name: swimtrack_front
        tasks_from: smoke
