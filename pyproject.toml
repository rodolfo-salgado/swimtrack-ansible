[project]
name = "swimtrack-ansible"
version = "0.1.0"
description = "Ansible deployment for the SwimTrack GPU inference service"
requires-python = ">=3.12,<3.13"
dependencies = [
    "ansible-core==2.21.0",
]

[tool.uv]
package = false
